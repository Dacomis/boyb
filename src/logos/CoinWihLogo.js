const CoinWihLogo = (coinName) => {
  console.log(coinName);
  return (
    // <div className="coin">
    //   <img src={coinLogo} className="coin_logo" alt={coinLogo} />
    //   <div className="name">{coinName}</div>
    // </div>
    <div> {coinName}</div>
  );
};

export default CoinWihLogo;
